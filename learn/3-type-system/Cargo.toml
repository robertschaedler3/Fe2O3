[package]
name = "type-system"
version = "0.1.0"
edition = "2021"

[features]
default = ["idiomatic"]
panic = []
explicit= []
idiomatic = []

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde_json = "1.0.117"
